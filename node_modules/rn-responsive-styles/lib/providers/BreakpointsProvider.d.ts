import * as React from 'react';
export declare type Breakpoints = [number, number, number, number];
export declare const useBreakpoints: () => Breakpoints;
declare type Props = {
    children: React.ReactNode;
    breakpoints: Breakpoints;
};
export declare function BreakpointsProvider({ children, breakpoints }: Props): JSX.Element;
export {};
