import { ReactNode } from 'react';
declare type Props = {
    children: ReactNode;
    placeholder?: ReactNode;
};
export declare function SSRProvider({ placeholder, children }: Props): JSX.Element;
export {};
